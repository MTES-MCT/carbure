#!/bin/sh

export PYTHONPATH=/build/${REQUEST_ID}/.apt/usr/lib/python3/dist-packages/:${PYTHONPATH}

# collect static
python3 ./web/manage.py collectstatic --noinput

# compile translations
python3 ./web/manage.py compilemessages