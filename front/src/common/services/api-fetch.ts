import createClient from "openapi-fetch"
import type { paths } from "common/services/carbure-api.schema" // generated by openapi-typescript

export const api = createClient<paths>({
  baseUrl: `${window.location.origin}/api`,
})
